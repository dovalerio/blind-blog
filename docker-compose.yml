services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rails_blog
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
    ports:
      - "127.0.0.1:3001:3000"
